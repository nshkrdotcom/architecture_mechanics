# reports/comparisons

One JSON per declared comparison: `control_run`, `candidate_runs`,
`primary_metric`, `matching_strategy`, and `permitted_differences` with a
justification for each. First written by prompt 12.
